# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1



orbs:
  # Declare pre0defined jobs
  welcome: circleci/welcome-orb@0.4.1
# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  # build-infra:
  #   docker:
  #     - image: amazon/aws-cli
  #   steps:
  #     - checkout
  #     - run: 
  #         name: "Build Infra using CF"
  #         command: |
  #           aws cloudformation deploy \
  #               --template-file 'Excercise 4/infra.yml' \
  #               --stack-name myStack-${CIRCLE_WORKFLOW_ID:0:5} \
  #               --region us-east-1

  config-infra:
    docker:
      - image: python:3.7-alpine3.11
    steps:
      - checkout
      - add_ssh_keys:
                fingerprints: ["64:40:a5:48:d0:c1:5f:94:f0:b8:2a:6c:bc:b4:f5:22"]
      - run:
          name: "Install Ansible"
          command: apk add --update ansible
      - run:
          name: "Change Directory"
          command: cd 'Excercise 5'
      - run:
          name: "Run Playbook"
          command: pwd; ansible-playbook -i inventory main.yml

  

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      # - build-infra
      - config-infra
